import { style } from '@vanilla-extract/css';

export const menuContainer = style({
  width: '100%',
});

export const ulList = style({
  listStyle: 'none',
  padding: 0,
  margin: 0,
  width: '100%',
});
